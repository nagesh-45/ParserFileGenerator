package com.xsdgenerator;

/**
 * Single source of truth for the build stamp shown in the UI, /version, and validation errors.
 * Bump this whenever a fix must be verifiable on a user machine.
 */
public final class AppBuild {

    public static final String ID = "2026-07-25g";

    private AppBuild() {
    }
}
